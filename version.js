/* Auto-stamped at deploy time by .github/workflows/pages.yml (git describe).
 * `self` is the page's window here AND the ServiceWorker global in sw.js, so the
 * same file feeds both the on-screen version label and the cache name. The value
 * committed to git is just a local-dev placeholder. */
self.__APP_VERSION__ = 'dev';
